<mujoco model="scene_lift">
  <include file="so101.xml" />

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0" />
    <rgba haze="0.15 0.25 0.35 1" />
    <global azimuth="160" elevation="-20" />
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512"
        height="3072" />
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4"
        rgb2="0.1 0.2 0.3"
        markrgb="0.8 0.8 0.8" width="300" height="300" />
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5"
        reflectance="0.2" />
  </asset>

  <worldbody>
    <light pos="0 0 3.5" dir="0 0 -1" directional="true" />
    <geom name="floor" size="0 0 0.05" pos="0 0 0" type="plane" material="groundplane" />

    <!-- Sponge stand-in: 6 x 4 x 2.5 cm box (kept named "cube" — every env/test
         references cube_geom/cube_joint). Real target is a dish sponge: high
         friction, soft contact (solref 0.01 vs the old rigid 0.001) but
         deliberately stiffer than real foam so the policy can't rely on
         squishiness; mass 8g is also well above a real sponge piece (~1g) so
         the real object is strictly easier to move. Spawn orientation (stands
         on a non-largest face) is sampled in base_env.reset. -->
    <body name="cube" pos="0.10 0.10 0.0125">
      <freejoint name="cube_joint"/>
      <geom name="cube_geom" type="box" size="0.03 0.02 0.0125" mass="0.008"
          rgba="0.95 0.85 0.25 1" condim="6" friction="1.5 0.01 0.001"
          contype="1" conaffinity="1" solref="0.01 1"/>
    </body>
  </worldbody>
</mujoco>

pyserial
evdev
scipy
seaborn  # calibration residual diagnostic plots (real/plot_calib_residuals.py)
pupil-apriltags
fastapi
uvicorn
watchfiles  # fast file watcher for `python -m panel --reload`
transformers  # SAM3 text prompting + SAM2 streaming mask tracking (real/sam_seg.py)
tqdm  # resumable offline dataset mask progress (real/tracking/eval_estimator.py)
# transformers' fast image/video processors also need torchvision; install the build
# matching the env's torch: pip install torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
